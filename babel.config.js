
// Export the Babel configuration
module.exports = {
    // Specify the preset to use for transpiling modern JavaScript (ES6+) to a version compatible with older environments
    presets: ["@babel/preset-env"],
};